# 本机 systemd 部署配置（WSL 专用，不入库）
dsh-web.service

# 日志
*.log
dsh-client-debug.log

# 构建产物
*.exe
nimcache/

# 依赖与数据
node_modules/
*.zstd
pet-state.json
platform-token.json

# 本地环境
.dsh/
term-state.json

# 运行时图片背景资源（不入库）
terminal-server/backgrounds/
